/**
 * Re-export shared types for dependency parsing
 */
export type {
  PackageManager,
  Dependency,
  DependencyManifest,
  PackageManagerConfig,
  DetectionResult,
} from '@context-guardian/types';
