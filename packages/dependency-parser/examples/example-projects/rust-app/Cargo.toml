[package]
name = "example-rust-app"
version = "0.1.0"
edition = "2021"

[dependencies]
serde = "1.0.195"
tokio = { version = "1.35.0", features = ["full"] }
axum = "0.7.0"

[dev-dependencies]
mockall = "0.12.0"
