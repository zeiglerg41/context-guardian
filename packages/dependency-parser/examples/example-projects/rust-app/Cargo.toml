[package]
name = "example-rust-app"
version = "0.1.0"
edition = "2021"

[dependencies]
serde = "1.0.195"
tokio = { version = "1.35.0", features = ["full"] }
axum = "0.7.0"
# Multi-line inline table (old parser broke on this)
reqwest = { version = "0.11.23", default-features = false, features = [
    "json",
    "rustls-tls",
] }
# Path dependency
my-utils = { path = "../my-utils" }

[dev-dependencies]
mockall = "0.12.0"

[build-dependencies]
cc = "1.0.83"
